def count_primes(n):
    """
    Counts the number of prime numbers less than a non-negative
    number (n) by using the sieve of Eratosthenes.
    
    >>> count_primes(1)
    0
    >>> count_primes(2)
    0
    >>> count_primes(3)
    1
    >>> count_primes(13)
    5
    >>> count_primes(10000)
    1229
    
    :see: https://en.wikipedia.org/wiki/Sieve_of_Eratosthenes
    :param n: int
    :returns: int
    """
    
    return 0
    
